#include "libcncore.h"

Object *${SYMBOL_NAME}(void)
{
    Object *obj = new_object();

    if (!obj)
        return (NULL);

    SET_PARENT_CLASS_BUILD(obj, ${PARENT_BUILDER_SYMBOL});
${METHODS}
// ${ATTRS}
    return (obj);
}
