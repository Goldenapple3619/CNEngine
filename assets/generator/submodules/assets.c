#include "../engine_main.h"

uint8_t load_assets_handler(Object *ctx)
{
    $template(ASSET_LIBS)

    return (0);
}

uint8_t load_entry_scene(Object *ctx)
{
    $template(LOAD_SCENE)

    return (0);
}
